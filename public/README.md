# Public assets

Place the following file here before going to production:

- `hero.mp4` — hero background video (1920×1080, ~8–20 sec loop, H.264, < 4 MB).
  - A poster image (`hero-poster.svg`) is already provided so the section renders correctly even without the video.

Optionally:
- `og.jpg` — Open Graph share image (1200×630).
- `apple-touch-icon.png` — 180×180.
